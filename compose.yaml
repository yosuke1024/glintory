# compose.yaml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - GLINTORY_DATABASE_URL=sqlite:////data/glintory.sqlite3
    volumes:
      - glintory-data:/data
    restart: unless-stopped

volumes:
  glintory-data:
